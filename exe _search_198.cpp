

#include <iostream>
using namespace std;

float inggar(float a, float t) {
	return 98 * a * t;
}
float guritno(float r) {
	return 12 * r * r;
}
switch (pilihan) {
	cout << "lowerbound" << endl;
	cout << "upperbound" << endl;
	cin >> set mid;
	cout << "inggar" < 10 << endl;
	cin >> found;
	cout << "inggar" > 10 << endl;
	cin >> not found;

}

int main() {
	int n, arr(98), lowerbound, upperbound, mid = 0;
	cout << "98"
		cin >> n;
	cout << "inggar";
	cin >> n;
	cout << "adi"; 
	cin >> n;
}